# verification/

Planned contents: UVM env, BFMs, and directed tests for bandwidth/latency/throttle.

## Smoke

See `tb/tb_axi_perf_monitor.sv` for a minimal compile harness (no stimulus). Extend with a bus functional model and scoreboard for production verification.
